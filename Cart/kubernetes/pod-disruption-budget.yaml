# Protects the deployment from being disrupted by terminating pods during downtime or maintenance

apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: cart-deployment
  namespace: prod
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app: cart
      app.kubernetes.io/component: cart
